# 2026-03-30 工作日誌：Qwen3-Coder Tool-Call 增強計畫

## 今日目標

在 Intel Core Ultra iGPU 上改善 Qwen3-Coder-30B-A3B-Instruct INT4 模型的**長上下文工具呼叫準確度**，目標是 24K-32K token 下保持穩定的 tool-call 能力。

---

## 背景摘要

### 問題

使用 INT4 量化的 Qwen3-Coder-30B-A3B 作為 coding agent 時，在 **24K+ token** 的 hard agentic 場景下工具呼叫會退化：
- 模型不再執行被要求的工具呼叫
- 改為「敘述分析」對話歷史中的程式碼
- 這是 **注意力層 INT4 量化** 造成的，NOT 路由器問題

### 已完成的實驗

| 實驗 | 結果 |
|---|---|
| 基線 INT4 基準測試 | NIAH 100%、簡單 tool-call 100%、**hard tool-call 在 24K+ 退化** |
| 路由器反量化 (INT4→FP16) | **無顯著改善** — 證實路由器不是瓶頸 |
| Gemini 分析審查 | 多數建議錯誤；唯一有價值的：長上下文校準資料 |

### 基線 Hard Tool-Call 分數

| 上下文長度 | 4K | 8K | 16K | 24K | 32K |
|---|---|---|---|---|---|
| INT4 基線 | 4.8/5 | 4.8/5 | **5.0/5** | 4.0/5 | 4.2/5 |

### 關鍵發現

1. **NIAH 和簡單 tool-call 不受 INT4 影響** — 到 32K 都完美
2. **Hard agentic tool-call 在 24K+ 退化** — 模型無法在大量對話歷史中優先處理最新指令
3. **MoE 路由器量化不是原因** — 48 個路由器反量化到 FP16 毫無效果
4. **注意力層是瓶頸** — 192 個 INT4 q/k/v/o_proj 層影響長上下文注意力模式
5. **LayerNorm 權重已是 FP32** — 不需要保護（Gemini 的建議不正確）
6. **embed_tokens/lm_head 已是 INT8** — 基線轉換已經保護了這些
7. **長上下文校準資料是關鍵缺口** — 標準短文本校準 (2K token) 無法讓 AWQ 學會在 24K 保護注意力權重

---

## 今日計畫

### 第一步：Tier 3 轉換 [進行中]

**狀態：** 已在背景執行

```bash
python scripts/convert_hq.py --tier tier3
```

**轉換參數：**
- 來源：本地 HF 權重 (`C:\working\models\Qwen3-Coder-30B-A3B-Instruct\HF`, 56.87 GB)
- 輸出：`C:\working\models\Qwen3-Coder-30B-A3B-Instruct\INT4-HQ-T3`
- 模式：INT4_ASYM
- Group size：64（基線是 128，精細度提高 2 倍）
- Ratio：0.9（90% INT4, 10% INT8）
- AWQ：開啟（Activation-aware Weight Quantization）
- Scale estimation：開啟
- 校準資料集：wikitext2, 128 筆樣本
- 敏感度指標：max_activation_variance
- All layers：開啟（所有層都參與敏感度排序）

**流程：**
```
HF 原始權重 (BF16, 56.87 GB)
  ↓ optimum-cli export openvino --weight-format int4
  ↓ 內部：先轉 FP16 IR → NNCF compress_weights
INT4-HQ-T3 (~15 GB)
```

**預估時間：** 2-4 小時

**限制：** CLI 不支援 `ignored_scope`，無法保護 MoE 路由器。但我們已證實路由器不是瓶頸。

### 第二步：Benchmark 比較

轉換完成後，跑 hard tool-call 基準測試：

```bash
python scripts/benchmark_context.py \
  --backend genai \
  --model-dir "C:\working\models\Qwen3-Coder-30B-A3B-Instruct\INT4-HQ-T3" \
  --device GPU --tests tool_call_hard --max-tokens 256 --quick
```

**預期對比：**

| 上下文 | INT4 基線 | INT4-HQ-T3 (目標) |
|---|---|---|
| 16K | 5.0/5 | 5.0/5 |
| 24K | 4.0/5 | 4.6+/5 |
| 32K | 4.2/5 | 4.6+/5 |

### 第三步（如果 Tier 3 不夠好）：Tier 1 轉換

```bash
python scripts/convert_hq.py --tier tier1 --dataset long_tool_calling --subset-size 32
```

**Tier 1 額外優勢：**
- 20K+ token 長上下文代理式校準資料（12 工具定義 + 多輪 tool call/result 歷史）
- MoE 路由器保護（`ignored_scope`）
- 可選 `hessian_input_activation` 敏感度指標

**這是殺手鐧** — 長上下文校準資料會讓 AWQ 在壓縮時觀察到 20K+ token 的注意力激活模式，從而自動保護在長上下文下最敏感的注意力權重。

### 第四步：完整基準測試

如果有改善，跑完整（非 quick）基準測試（8 個上下文長度 × 3 個測試套件）：

```bash
python scripts/benchmark_context.py --compare "...\INT4" "...\INT4-HQ-T3" \
  --tests niah tool_call tool_call_hard
```

---

## 檔案與資源

| 項目 | 路徑 |
|---|---|
| 專案目錄 | `C:\working\qwen3-model-tool-call-enhancement` |
| 腳本 | `scripts/convert_hq.py`, `benchmark_context.py`, `dequant_routers.py` |
| HF 原始權重 | `C:\working\models\Qwen3-Coder-30B-A3B-Instruct\HF` (56.87 GB) |
| 基線 INT4 | `C:\working\models\Qwen3-Coder-30B-A3B-Instruct\INT4` (15.19 GB) |
| 路由器 FP16 | `C:\working\models\Qwen3-Coder-30B-A3B-Instruct\INT4-RouterFP16` (15.22 GB) |
| Tier 3 輸出 | `C:\working\models\Qwen3-Coder-30B-A3B-Instruct\INT4-HQ-T3` (轉換中) |
| 基準測試結果 | `benchmarks/` 目錄下的 JSON + CSV |
| Python venv | `C:\working\openvino-notebook\llm-code-assistant-openvino\llm-code-assistant-venv` |

### 環境

- **硬體：** Intel Core Ultra, iGPU (Integrated), 32 GB RAM, iGPU memory override 27.5 GB
- **軟體：** OpenVINO 2026.0, GenAI 2026.0, NNCF 3.0.0, optimum-intel 1.27.0.dev0
- **磁碟空間：** ~259 GB 可用

---

## NNCF 可用的敏感度指標

| 指標 | 說明 | 適用場景 |
|---|---|---|
| `max_activation_variance` | 最大激活值方差 | **預設首選** — 識別激活值波動最大的層 |
| `mean_activation_variance` | 平均激活值方差 | 較穩定但可能遺漏極端情況 |
| `mean_activation_magnitude` | 平均激活值幅度 | 適合注意力層 |
| `hessian_input_activation` | 基於 Hessian 的輸入激活分析 | **最精確但最慢** — 計算每個權重對損失的影響 |
| `weight_quantization_error` | 純權重量化誤差 | 不需要校準資料，最快但最粗糙 |

---

## Gemini 分析審查摘要

| Gemini 建議 | 判定 | 我們的證據 |
|---|---|---|
| 保護 `gate_proj` | **錯誤** — 搞混 SwiGLU 門控和 MoE 路由器 | 路由器是 `mlp.gate`，不是 `gate_proj` |
| 保護 LayerNorm | **不必要** | 已驗證：LayerNorm 權重是 FP32 [1,1,2048] |
| 保護 embed/lm_head | **已做到** | 基線已是 INT8 |
| `hessian_trace` 指標 | **不存在** | NNCF 有 `HESSIAN_INPUT_ACTIVATION`（不同） |
| Group size 64 | **有效** ✅ | 已納入計畫 |
| AWQ + scale_estimation | **有效** ✅ | 已納入計畫 |
| 20K+ 校準資料 | **非常有價值** ✅ | **已採納** — 新增 `long_tool_calling` 資料集 |
| Ratio 0.85 | **值得測試** | 可用 `--ratio 0.85` 實驗 |
